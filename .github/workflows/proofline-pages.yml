name: Validate and deploy Proofline Projects subsite

on:
  pull_request:
    branches: [main]
    paths:
      - "proofline-portfolio/**"
      - ".github/workflows/proofline-pages.yml"
  push:
    branches: [main]
    paths:
      - "proofline-portfolio/**"
      - ".github/workflows/proofline-pages.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  validate:
    name: Validate immutable portfolio release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Reconstruct, verify, and inspect
        shell: bash
        run: bash proofline-portfolio/build-pages-subsite.sh

  deploy:
    name: Deploy GitHub Pages
    if: github.event_name != 'pull_request'
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}proofline-projects/
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Build verified subsite artifact
        shell: bash
        run: bash proofline-portfolio/build-pages-subsite.sh

      - name: Configure Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: _site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
