[mariadb]
# Conservative baseline for a container-local application database.
# Site-specific sizing and durability changes require backup/restore testing.
skip_name_resolve=ON
character_set_server=utf8mb4
collation_server=utf8mb4_unicode_ci
max_connections=100

[client]
default_character_set=utf8mb4
